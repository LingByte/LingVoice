{
  "title": "创建聊天补全",
  "subtitle": "POST /v1/chat/completions：与 OpenAI Chat Completions 协议对齐。",
  "blocks": [
    {
      "type": "markdown",
      "content": "本接口与 OpenAI **Chat Completions** 规范一致；请求体须包含 `model` 与 `messages` 等必填字段，其余可选字段行为请参阅 OpenAPI 模式定义。"
    },
    {
      "type": "openapiOperation",
      "method": "POST",
      "path": "/chat/completions",
      "showTry": true
    }
  ]
}
